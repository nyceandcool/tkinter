from tkinter import *
from tkinter import ttk
root =Tk() # creates the window
button = ttk.Button(root, text = 'Click Me')
button.pack()
#button['text'] = 'Press Me' # command to change the label
#button.config(text = 'Push Them') # another command line to change the button label
# Tk geometry manager object controls where things can be placed
#Three geometry manager 1. pack 2. grid 3. place
